package Lista14;

public class Questao1 {
	/*
	 * É um objeto que implementa uma interface específica para capturar
	 * e responder a eventos gerados pelos componentes da interface
	 * gráfica do usuário (GUI). Os ouvintes são usados para lidar com
	 * interações do usuário.
	 * 
	 * No padrão Observer, o sujeito (também conhecido como objeto
	 * observável) mantém uma lista de observadores (também
	 * conhecidos como objetos dependentes) e notifica todos eles
	 * quando ocorre uma mudança de estado. Isso permite que os
	 * observadores respondam dinamicamente a essas alterações.
	 */
}